// import Rest from "../services/_rest";
import { describe, it } from "mocha";
import assert from "assert";

describe("Main describe", () => {
  it("Sample test", function () {
    assert.equal([1, 2, 3].indexOf(4), -1);
  });
});
